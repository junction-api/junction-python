## 0.1.0 - 2026-05-04
### Changed
* **`JunctionEnvironment`** base URLs updated across all environments (`PRODUCTION`, `PRODUCTION_EU`, `SANDBOX`, `SANDBOX_EU`) to point to the new `junction.com` domain instead of `tryvital.io`.
### Added
* **`MealInDbBaseClientFacingSource.calendar_date`** — new `str` field exposing the meal date in `YYYY-mm-dd` format, useful for providers that only record a calendar date rather than a full timestamp.

## 0.0.1 - 2026-05-01
* Initial SDK generation
* 🌿 Generated with Fern

